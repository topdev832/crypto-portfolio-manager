const config = {
  plugins: {
    // Tailwind v4 moved the PostCSS plugin to a separate package. Use '@tailwindcss/postcss'.
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
};

export default config;
